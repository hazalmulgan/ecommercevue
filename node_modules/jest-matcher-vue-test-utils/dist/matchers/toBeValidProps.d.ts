import Vue from "vue";
import { MatcherResult, MatcherComponent, MatcherComponentOptions } from "../utils";
interface ComponentProp {
    [name: string]: any;
}
declare global {
    namespace jest {
        interface Matchers<R, T> {
            /**
             * Asserts that the component accepts the set of props
             * @param {any} props - The set of props
             * @param options - Mount Option of the component
             * @example
             * expect(AComponent).toBeValidProps({ type: "the type", color: "cool one"})
             */
            toBeValidProps(props: ComponentProp, options?: MatcherComponentOptions<Vue>): R;
        }
    }
}
export default function toBeValidProps<V extends Vue>(received: MatcherComponent<V>, props: ComponentProp, dynamicMountOptions?: MatcherComponentOptions<V>): MatcherResult;
export {};
